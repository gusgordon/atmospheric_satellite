numpy<2
pandas<2
scipy
matplotlib
numba
jupyter
nbconvert
nbclient

# Optional: only needed to regenerate solar_graph_day_*.csv.
# The upstream package currently needs packaging/C-extension fixes on Python 3.11.
# git+https://github.com/gusgordon/airmass.git#egg=airmass
